import React from 'react'
import Kubernetes from '@public/icons/kubernetes_icon.svg'

const KubernetesIcon = ({className}) => {
  return (
    <Kubernetes className={className} />
  )
}

export default KubernetesIcon