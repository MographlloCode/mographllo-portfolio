import React from 'react'
import Tailwind from '@public/icons/tailwind_icon.svg'

const TailwindIcon = ({className}) => {
  return (
    <Tailwind className={className} />
  )
}

export default TailwindIcon