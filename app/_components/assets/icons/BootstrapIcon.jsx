import React from 'react'
import Bootstrap from '@public/icons/bootstrap_icon.svg'

const BootstrapIcon = ({className}) => {
  return (
    <Bootstrap className={className} />
  )
}

export default BootstrapIcon